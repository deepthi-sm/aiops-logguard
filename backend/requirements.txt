# Each PR appends only what it needs (asyncpg, redis, torch, etc.).

# Step 1 — API skeleton
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Step 3 — training pipeline.
drain3==0.9.11
torch==2.5.1
sentence-transformers==3.3.1
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
faiss-cpu==1.9.0

# Tooling — run by CI (.github/workflows/ci.yml)
ruff==0.7.2
mypy==1.13.0
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

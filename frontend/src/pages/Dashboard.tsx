/**
 * Dashboard (/) — built out in Step 4 (KpiBar + TimelineChart + Recent activity).
 * Step 2 deliverable is just the eyebrow + title so navigation works.
 */
export function Dashboard() {
  return (
    <div>
      <div className="text-[11px] uppercase tracking-[0.08em] text-tertiary">
        Anomaly intelligence
      </div>
      <h1 className="mt-1 text-[22px] font-medium tracking-[-0.01em] text-primary">
        Live dashboard
      </h1>
    </div>
  );
}
